var Controller = require("./../cloudcase/Controller");
var ModelOperation = require("./../cloudcase/ModelOperation"); 
var Model = require("./../cloudcase/Model");
var cloudcase = {
	Model : Model,
	Controller : Controller,
	ModelOperation : ModelOperation
};

module.exports = cloudcase;
